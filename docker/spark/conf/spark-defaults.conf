# Spark Configuration for Playback Event Platform
# ==============================================

# Application defaults
# Use local mode for Iceberg compatibility (cluster mode has JAR conflicts)
spark.master                                    local[*]
spark.driver.memory                             1g
spark.executor.memory                           1g

# S3/MinIO Configuration
spark.hadoop.fs.s3a.endpoint                    http://minio:9000
spark.hadoop.fs.s3a.access.key                  minioadmin
spark.hadoop.fs.s3a.secret.key                  minioadmin123
spark.hadoop.fs.s3a.path.style.access           true
spark.hadoop.fs.s3a.impl                        org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled      false

# Iceberg Configuration - using S3FileIO for AWS compatibility
spark.sql.extensions                            org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.iceberg                       org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg.type                  rest
spark.sql.catalog.iceberg.uri                   http://iceberg-rest:8181
spark.sql.catalog.iceberg.warehouse             s3://warehouse/
spark.sql.catalog.iceberg.io-impl               org.apache.iceberg.aws.s3.S3FileIO
spark.sql.catalog.iceberg.s3.endpoint           http://minio:9000
spark.sql.catalog.iceberg.s3.path-style-access  true

# Default catalog
spark.sql.defaultCatalog                        iceberg

# Event logging DISABLED
spark.eventLog.enabled                          false

# Performance
spark.sql.shuffle.partitions                    4
spark.default.parallelism                       4
spark.sql.adaptive.enabled                      true

# Serialization
spark.serializer                                org.apache.spark.serializer.KryoSerializer

# UI
spark.ui.enabled                                true

# Packages - Iceberg 1.5.0 compatible versions (AWS SDK 2.20.18)
spark.jars.packages                             org.apache.iceberg:iceberg-spark-runtime-3.5_2.12:1.5.0,org.apache.iceberg:iceberg-aws:1.5.0,software.amazon.awssdk:bundle:2.20.18,software.amazon.awssdk:url-connection-client:2.20.18
