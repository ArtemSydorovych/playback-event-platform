package com.netflix.playback.model;

/**
 * Types of devices that can emit playback events.
 */
public enum DeviceType {
    IOS,
    ANDROID,
    WEB,
    SMART_TV,
    GAMING_CONSOLE,
    SET_TOP_BOX
}
